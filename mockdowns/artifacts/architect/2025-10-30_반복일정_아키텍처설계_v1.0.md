# 아키텍처 설계서

## 📋 기본 정보

- **작성일**: 2025-10-30
- **프로젝트명**: 반복 일정 기능 추가
- **버전**: v1.0
- **작성자**: BMAD Architect

## 🔍 프로젝트 구조 분석 결과

### 📦 기술 스택

- **프론트엔드**: 
  - React 19.1.0 + TypeScript 5.2.2
  - Vite 7.0.2 (빌드 도구)
  - MUI (Material-UI) 7.2.0 (UI 라이브러리)
  - Framer Motion 12.23.0 (애니메이션)
  - notistack 3.0.2 (알림)

- **테스트**: 
  - Vitest 3.2.4
  - React Testing Library 16.3.0
  - @testing-library/user-event 14.5.2
  - @testing-library/jest-dom 6.6.3

- **백엔드**: 
  - Express 4.19.2 (간단한 REST API 서버)
  - MSW 2.10.3 (Mock Service Worker)

### 🗂️ 현재 프로젝트 구조

```
src/
├── __tests__/           # 테스트 파일
│   ├── hooks/          # 커스텀 훅 테스트
│   ├── unit/           # 유닛 테스트
│   └── integration/    # 통합 테스트
├── apis/               # API 호출 함수
├── hooks/              # 커스텀 훅
│   ├── useEventForm.ts
│   ├── useEventOperations.ts
│   ├── useCalendarView.ts
│   ├── useNotifications.ts
│   └── useSearch.ts
├── utils/              # 유틸리티 함수
│   ├── dateUtils.ts
│   ├── eventUtils.ts
│   ├── eventOverlap.ts
│   ├── notificationUtils.ts
│   └── timeValidation.ts
├── types.ts            # 타입 정의
└── App.tsx             # 메인 컴포넌트
```

### 🔑 핵심 기존 구조 분석

#### 1. 타입 시스템 (types.ts)
```typescript
// ✅ 이미 반복 일정 타입 구조가 준비되어 있음
type RepeatType = 'none' | 'daily' | 'weekly' | 'monthly' | 'yearly';

interface RepeatInfo {
  type: RepeatType;
  interval: number;
  endDate?: string;
}

interface EventForm {
  // ... 기본 필드
  repeat: RepeatInfo;
}
```

#### 2. 상태 관리 패턴
- **커스텀 훅 기반**: React Hooks를 활용한 상태 관리
- **관심사 분리**: 폼(useEventForm), 연산(useEventOperations), 뷰(useCalendarView) 분리
- **API 통신**: useEventOperations에서 REST API 호출 담당

#### 3. 유틸리티 구조
- **dateUtils.ts**: 날짜 계산, 포맷팅, 범위 체크
- **eventUtils.ts**: 이벤트 필터링, 검색
- **timeValidation.ts**: 시간 유효성 검사

## 🏗️ 반복 일정 아키텍처 설계

### 📐 시스템 구성도

```
┌─────────────────────────────────────────────────────────────┐
│                         App.tsx                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  useEventForm (폼 상태 + 반복 설정)                    │ │
│  └───────────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  useEventOperations (CRUD + 반복 일정 처리)           │ │
│  │    - 반복 일정 생성/수정/삭제 로직                     │ │
│  └───────────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  Calendar View (반복 일정 표시)                       │ │
│  │    - 반복 일정 아이콘 표시                            │ │
│  │    - 여러 인스턴스 렌더링                             │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                   utils/repeatUtils.ts                      │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  반복 일정 계산 로직 (새로 생성 필요)                  │ │
│  │  - generateRepeatDates(): 반복 날짜 생성              │ │
│  │  - isLeapYear(): 윤년 체크                            │ │
│  │  - getDaysInMonth(): 월별 일수 계산                   │ │
│  │  - calculateNextOccurrence(): 다음 반복 날짜          │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│              Backend API (server.js + handlers.ts)          │
│  - POST /api/events (반복 일정 생성)                        │
│  - PUT /api/events/:id (단일/전체 수정)                     │
│  - DELETE /api/events/:id (단일/전체 삭제)                  │
└─────────────────────────────────────────────────────────────┘
```

### 🔗 핵심 컴포넌트 설계

#### 1. **반복 일정 계산 모듈** (utils/repeatUtils.ts - 신규 생성)
- **역할**: 반복 규칙에 따른 날짜 생성 로직
- **핵심 함수**:
  - `generateRepeatDates(event: Event): Date[]` - 반복 날짜 배열 생성
  - `isValidRepeatDate(date: Date, originalDate: Date, repeatType: RepeatType): boolean` - 유효한 반복 날짜 체크
  - `handleSpecialCases(date: Date, repeatType: RepeatType): Date | null` - 특수 케이스 처리 (31일, 윤년 2/29)
- **책임**:
  - 매일/매주/매월/매년 반복 로직 구현
  - 종료일까지의 모든 반복 날짜 계산
  - 월별 말일, 윤년 처리 등 특수 케이스 처리

#### 2. **반복 일정 데이터 모델** (types.ts - 확장)
```typescript
// Ai Edit
// 기존 타입 확장 필요
interface Event extends EventForm {
  id: string;
  repeatGroupId?: string;  // 반복 일정 그룹 식별자 (신규)
  isRepeatInstance?: boolean;  // 반복 인스턴스 여부 (신규)
  originalEventId?: string;  // 원본 이벤트 ID (단일 수정 시) (신규)
}
```

#### 3. **반복 일정 UI 컴포넌트** (App.tsx - 수정)
- **반복 설정 폼**: 이미 구조 존재, 로직 연결 필요
- **반복 일정 표시**: 캘린더에 반복 아이콘 추가
- **수정/삭제 확인 다이얼로그**: "해당 일정만/전체" 선택 UI

#### 4. **반복 일정 연산 훅** (hooks/useEventOperations.ts - 수정)
- **기능 확장**:
  - 반복 일정 생성 시 모든 인스턴스 생성
  - 단일 수정: 해당 인스턴스만 단일 일정으로 변환
  - 전체 수정: 모든 인스턴스 업데이트
  - 단일 삭제: 해당 인스턴스만 삭제
  - 전체 삭제: 모든 인스턴스 삭제

## 🛡️ 아키텍처 가드레일

### 구조적 변경 원칙

- ✅ **기존 타입 수정 금지**: `RepeatType`, `RepeatInfo`는 이미 정의됨, 확장만 허용
- ✅ **기존 유틸 함수 수정 금지**: dateUtils.ts의 기존 함수는 수정하지 않고 새 함수 추가만
- ✅ **커스텀 훅 구조 유지**: useEventForm, useEventOperations의 기본 구조 유지
- ✅ **테스트 구조 준수**: `src/__tests__/unit/`, `src/__tests__/hooks/` 구조 유지

### 행동적 변경 원칙

- ✅ **TDD 필수**: 모든 새 함수는 테스트 먼저 작성
- ✅ **방어적 프로그래밍**: 날짜 계산 시 예외 처리 필수
- ✅ **타입 안정성**: 모든 함수에 명확한 타입 지정
- ✅ **단일 책임**: 각 함수는 하나의 역할만 수행

## 🔄 데이터 플로우

### 1. 반복 일정 생성 플로우

```
사용자 입력 (반복 설정)
    ↓
useEventForm (반복 정보 수집)
    ↓
saveEvent 호출 (useEventOperations)
    ↓
generateRepeatDates (반복 날짜 계산)
    ↓
POST /api/events (서버에 저장)
    ↓
fetchEvents (전체 일정 재조회)
    ↓
Calendar View (반복 일정 표시)
```

### 2. 반복 일정 수정 플로우

```
사용자가 반복 일정 클릭 → 수정
    ↓
"해당 일정만/전체" 다이얼로그 표시
    ↓
[ 해당 일정만 ]              [ 전체 ]
    ↓                           ↓
단일 일정으로 변환         repeatGroupId로 전체 조회
repeat.type = 'none'            ↓
    ↓                      모든 인스턴스 업데이트
PUT /api/events/:id            ↓
                         PUT /api/events (전체)
```

### 3. 반복 일정 삭제 플로우

```
사용자가 반복 일정 클릭 → 삭제
    ↓
"해당 일정만/전체" 다이얼로그 표시
    ↓
[ 해당 일정만 ]              [ 전체 ]
    ↓                           ↓
해당 인스턴스만 삭제     repeatGroupId로 전체 조회
    ↓                           ↓
DELETE /api/events/:id    모든 인스턴스 삭제
                              ↓
                      DELETE /api/events (전체)
```

## 🔗 인터페이스 정의

### API 계약 (기존 확장)

```typescript
// Ai Edit
// POST /api/events - 반복 일정 생성
Request Body: {
  ...EventForm,
  repeat: {
    type: 'daily' | 'weekly' | 'monthly' | 'yearly',
    interval: number,
    endDate: string  // 'YYYY-MM-DD' 형식
  }
}

Response: {
  success: boolean;
  events: Event[];  // 생성된 모든 반복 일정 인스턴스
}

// PUT /api/events/:id - 반복 일정 수정
Request Body: {
  ...EventForm,
  updateAll: boolean  // true: 전체 수정, false: 단일 수정
}

// DELETE /api/events/:id - 반복 일정 삭제
Query Params: {
  deleteAll: boolean  // true: 전체 삭제, false: 단일 삭제
}
```

### 데이터 모델 (확장)

```typescript
// Ai Edit
// utils/repeatUtils.ts에서 사용할 타입
interface RepeatDateGenerationOptions {
  startDate: string;
  repeatType: RepeatType;
  interval: number;
  endDate: string;
}

interface RepeatDateGenerationResult {
  dates: string[];  // 'YYYY-MM-DD' 형식 배열
  totalCount: number;
}
```

## 🚨 에러 처리 전략

### 반복 날짜 계산 에러
```typescript
// Ai Edit
// 1. 잘못된 날짜 형식
if (!isValidDate(startDate)) {
  throw new Error('유효하지 않은 시작 날짜입니다.');
}

// 2. 종료일이 시작일보다 이전
if (new Date(endDate) < new Date(startDate)) {
  throw new Error('종료일은 시작일보다 이후여야 합니다.');
}

// 3. 반복 간격이 비정상
if (interval < 1) {
  throw new Error('반복 간격은 1 이상이어야 합니다.');
}

// 4. 최대 2025-12-31까지만 허용
if (new Date(endDate) > new Date('2025-12-31')) {
  throw new Error('종료일은 2025-12-31까지만 설정 가능합니다.');
}
```

### 특수 케이스 처리
```typescript
// Ai Edit
// 매월 31일 → 31일이 없는 달은 건너뛰기
// 매년 2월 29일 → 윤년이 아닌 해는 건너뛰기
function handleSpecialCases(
  date: Date, 
  originalDay: number, 
  repeatType: RepeatType
): Date | null {
  // 월별 반복 시 해당 월에 날짜가 없으면 null 반환
  if (repeatType === 'monthly' || repeatType === 'yearly') {
    const daysInMonth = getDaysInMonth(
      date.getFullYear(), 
      date.getMonth() + 1
    );
    
    if (originalDay > daysInMonth) {
      return null;  // 이 날짜는 생성하지 않음
    }
  }
  
  return date;
}
```

## 📊 구현 우선순위

### Phase 1: 반복 날짜 계산 로직 (핵심)
1. ✅ `utils/repeatUtils.ts` 생성
2. ✅ 날짜 계산 유닛 테스트 작성
3. ✅ 반복 날짜 생성 함수 구현

### Phase 2: 반복 일정 생성
1. ✅ `useEventOperations` 확장
2. ✅ 반복 일정 저장 로직 구현
3. ✅ 통합 테스트 작성

### Phase 3: 반복 일정 표시
1. ✅ Calendar View에 반복 아이콘 추가
2. ✅ 반복 인스턴스 렌더링 로직
3. ✅ UI 테스트 작성

### Phase 4: 반복 일정 수정/삭제
1. ✅ 단일/전체 선택 다이얼로그 추가
2. ✅ 수정/삭제 로직 구현
3. ✅ 시나리오 테스트 작성

## 🔄 다음 단계

- ✅ Analyst 에이전트에게 상세 요구사항 분석 요청
- ✅ 반복 날짜 계산 로직의 테스트 케이스 설계
- ✅ Dev 에이전트에게 구현 가이드라인 전달

## ✅ 품질 보증 체크리스트

- ✅ 모든 컴포넌트의 역할이 명확함
- ✅ 인터페이스가 정의됨
- ✅ 가드레일이 설정됨
- ✅ 기술 스택이 결정됨 (기존 스택 활용)
- ✅ 데이터 플로우가 명확함
- ✅ 에러 처리 전략이 수립됨
- ✅ 다음 에이전트가 구현할 수 있는 충분한 정보 제공

---

## 📈 점수 현황 (Score Status)

- **획득 점수 (Acquired Score):** 7점 (체크리스트 7개 항목 완료)
- **누적 점수 (Cumulative Score):** 7점 (이전 에이전트 0점 + 현재 7점)
- **총점 (Total Score):** 예상 80점 (Architect 7 + Analyst 5 + Dev 50 + QA 18)

---

**Architect 작업 완료**: 프로젝트 구조 분석 및 아키텍처 설계가 완료되었습니다. 다음 Analyst 에이전트가 상세 요구사항을 분석할 수 있도록 충분한 기술적 기반을 제공했습니다.

