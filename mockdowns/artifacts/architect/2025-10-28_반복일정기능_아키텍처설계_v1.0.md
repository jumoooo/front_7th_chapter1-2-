# 🏗️ 아키텍처 설계서 - 반복 일정 기능

## 📋 기본 정보

- **작성일**: 2025-10-28
- **프로젝트명**: 캘린더 애플리케이션 - 반복 일정 기능
- **버전**: v1.0
- **작성자**: BMAD Architect

---

## 🏗️ 시스템 아키텍처

### 📐 전체 구조도

```
┌─────────────────────────────────────────────────────────────────┐
│                        Presentation Layer                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  App.tsx (Main Component)                                │   │
│  │  - 이벤트 폼 렌더링                                       │   │
│  │  - 캘린더 뷰 렌더링 (Week/Month)                         │   │
│  │  - 일정 목록 렌더링                                       │   │
│  │  - 반복 일정 수정/삭제 다이얼로그                        │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ↓  ↑
┌─────────────────────────────────────────────────────────────────┐
│                    State Management Layer (Hooks)                │
│  ┌────────────────┐  ┌──────────────────┐  ┌─────────────────┐ │
│  │useEventForm    │  │useEventOperations│  │useCalendarView  │ │
│  │- 폼 상태 관리  │  │- CRUD 작업       │  │- 뷰 관리        │ │
│  │- 반복 설정     │  │- 반복 일정 확장  │  │- 날짜 네비게이션│ │
│  └────────────────┘  └──────────────────┘  └─────────────────┘ │
│  ┌────────────────┐  ┌──────────────────┐                       │
│  │useSearch       │  │useNotifications  │                       │
│  │- 검색/필터링   │  │- 알림 관리       │                       │
│  └────────────────┘  └──────────────────┘                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓  ↑
┌─────────────────────────────────────────────────────────────────┐
│                  Business Logic Layer (Utils)                    │
│  ┌────────────────┐  ┌──────────────────┐  ┌─────────────────┐ │
│  │dateUtils.ts    │  │eventUtils.ts     │  │eventOverlap.ts  │ │
│  │- 날짜 계산     │  │- 이벤트 필터링   │  │- 겹침 검증      │ │
│  │- 반복 날짜 생성│  │- 반복 일정 확장  │  │(반복 일정 제외) │ │
│  └────────────────┘  └──────────────────┘  └─────────────────┘ │
│  ┌────────────────┐  ┌──────────────────┐                       │
│  │timeValidation  │  │notificationUtils │                       │
│  │- 시간 검증     │  │- 알림 계산       │                       │
│  └────────────────┘  └──────────────────┘                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓  ↑
┌─────────────────────────────────────────────────────────────────┐
│                       Data Layer (Types & API)                   │
│  ┌────────────────┐  ┌──────────────────┐                       │
│  │types.ts        │  │server.js         │                       │
│  │- Event         │  │- Express API     │                       │
│  │- EventForm     │  │- Mock 데이터     │                       │
│  │- RepeatInfo    │  │- CRUD 엔드포인트 │                       │
│  └────────────────┘  └──────────────────┘                       │
└─────────────────────────────────────────────────────────────────┘
```

---

### 🧩 핵심 컴포넌트

#### 1️⃣ Presentation Layer

**App.tsx**

- **역할**: 메인 UI 컴포넌트
- **책임**:
  - 이벤트 생성/수정 폼 렌더링
  - 캘린더 뷰 (Week/Month) 렌더링
  - 일정 목록 렌더링
  - 반복 일정 수정/삭제 확인 다이얼로그 관리
- **신규 추가 사항**:
  - ✅ 반복 일정 UI 활성화 (441-478 라인)
  - ✅ 반복 아이콘 표시 (MUI Repeat 아이콘)
  - ✅ "해당 일정만 수정/삭제하시겠어요?" 다이얼로그

---

#### 2️⃣ State Management Layer (Hooks)

**useEventForm.ts**

- **역할**: 이벤트 폼 상태 관리
- **책임**:
  - 폼 입력 데이터 관리 (title, date, time, description 등)
  - 반복 설정 상태 관리 (isRepeating, repeatType, repeatInterval, repeatEndDate)
  - 시간 유효성 검증
- **현재 상태**: 반복 관련 상태 이미 구현됨 ✅
- **변경 사항**: 없음

**useEventOperations.ts**

- **역할**: 이벤트 CRUD 작업
- **책임**:
  - 이벤트 생성 (POST /api/events)
  - 이벤트 수정 (PUT /api/events/:id)
  - 이벤트 삭제 (DELETE /api/events/:id)
  - 이벤트 목록 로드 (GET /api/events)
- **신규 추가 사항**:
  - ✅ `expandRecurringEventsInView()`: 현재 뷰 범위의 반복 일정 확장
  - ✅ `updateSingleEvent()`: 단일 반복 일정 수정
  - ✅ `updateAllEvents()`: 전체 반복 일정 수정
  - ✅ `deleteSingleEvent()`: 단일 반복 일정 삭제
  - ✅ `deleteAllEvents()`: 전체 반복 일정 삭제

**useCalendarView.ts**

- **역할**: 캘린더 뷰 관리
- **책임**:
  - 뷰 타입 관리 (week/month)
  - 현재 날짜 관리
  - 네비게이션 (이전/다음)
  - 공휴일 데이터 로드
- **신규 추가 사항**:
  - ✅ 반복 일정 확장 로직 통합

**useSearch.ts**

- **역할**: 검색 및 필터링
- **책임**:
  - 검색어 기반 이벤트 필터링
  - 날짜 범위 기반 필터링
- **신규 추가 사항**:
  - ✅ 확장된 반복 일정에서 검색

**useNotifications.ts**

- **역할**: 알림 관리
- **책임**:
  - 예정된 이벤트 알림 표시
  - 알림 목록 관리
- **신규 추가 사항**:
  - ✅ 반복 일정의 알림 처리

---

#### 3️⃣ Business Logic Layer (Utils)

**dateUtils.ts**

- **역할**: 날짜 관련 유틸리티
- **책임**:
  - 날짜 계산 (getDaysInMonth, getWeekDates, getWeeksAtMonth)
  - 날짜 포맷팅 (formatDate, formatMonth, formatWeek)
  - 날짜 범위 검증 (isDateInRange)
- **신규 추가 함수**:
  - ✅ `generateRecurringDates(startDate, repeatType, interval, endDate)`: 반복 날짜 생성
  - ✅ `isValidRecurringDate(date, originalDate, repeatType)`: 특수 날짜 유효성 검증
  - ✅ `calculateNextRecurringDate(currentDate, repeatType, interval)`: 다음 반복 날짜 계산
  - ✅ `isLeapYear(year)`: 윤년 판별
  - ✅ `getDaysInMonthForDate(date)`: 특정 날짜의 월 일수

**eventUtils.ts**

- **역할**: 이벤트 관련 유틸리티
- **책임**:
  - 이벤트 필터링 (날짜 범위, 검색어)
  - 이벤트 검색
- **신규 추가 함수**:
  - ✅ `expandRecurringEvents(events, startDate, endDate)`: 반복 일정을 개별 이벤트로 확장
  - ✅ `filterRecurringEvents(events)`: 반복 일정만 필터링
  - ✅ `createRecurringInstance(baseEvent, instanceDate)`: 반복 인스턴스 생성

**eventOverlap.ts**

- **역할**: 이벤트 겹침 검증
- **책임**:
  - 두 이벤트의 시간 겹침 확인
  - 겹치는 이벤트 목록 반환
- **변경 사항**:
  - ℹ️ 과제 요구사항에 따라 반복 일정은 겹침 검증 제외

**timeValidation.ts**

- **역할**: 시간 유효성 검증
- **책임**:
  - 시작/종료 시간 검증
- **변경 사항**: 없음

**notificationUtils.ts**

- **역할**: 알림 유틸리티
- **책임**:
  - 예정된 이벤트 필터링
  - 알림 메시지 생성
- **신규 추가 사항**:
  - ✅ 확장된 반복 일정의 알림 처리

---

#### 4️⃣ Data Layer

**types.ts**

- **역할**: TypeScript 타입 정의
- **현재 타입**:
  - `RepeatType`: 'none' | 'daily' | 'weekly' | 'monthly' | 'yearly'
  - `RepeatInfo`: { type, interval, endDate? }
  - `EventForm`: 이벤트 폼 데이터
  - `Event`: EventForm + id
- **신규 추가 타입** (선택적):
  - ✅ `RecurringEventInstance`: 확장된 반복 일정 인스턴스 (클라이언트 전용)
    ```typescript
    interface RecurringEventInstance extends Event {
      isRecurring: boolean;
      instanceDate: string;
      originalEventId: string;
    }
    ```
  - ✅ `parentEventId?: string`: 반복 그룹 식별 (Event 타입에 추가, 선택적)
  - ✅ `exceptions?: string[]`: 예외 날짜 목록 (Event 타입에 추가, 선택적)

**server.js**

- **역할**: Express Mock 서버
- **책임**:
  - 이벤트 CRUD API 제공
  - Mock 데이터 관리
- **변경 사항**:
  - ℹ️ 반복 일정 확장은 클라이언트 측에서 처리
  - ℹ️ 서버는 기준 이벤트만 저장

---

## 🔗 인터페이스 정의

### 📡 API 계약

#### 기존 API (변경 없음)

```typescript
// 이벤트 목록 조회
GET /api/events
Response: { events: Event[] }

// 이벤트 생성
POST /api/events
Request: EventForm
Response: Event

// 이벤트 수정
PUT /api/events/:id
Request: EventForm (또는 부분 수정 데이터)
Response: Event

// 이벤트 삭제
DELETE /api/events/:id
Response: { success: boolean }
```

#### 반복 일정 처리 전략

**Option 1: 클라이언트 측 확장 (권장)**

- 서버는 기준 이벤트(base event)만 저장
- 클라이언트가 반복 일정을 동적으로 확장
- 장점: API 변경 불필요, 서버 부하 감소
- 단점: 클라이언트 로직 복잡도 증가

**Option 2: 서버 측 확장**

- 서버가 반복 인스턴스를 모두 저장
- 클라이언트는 단순히 목록 조회
- 장점: 클라이언트 로직 단순
- 단점: 데이터 중복, 저장 공간 증가

**선택**: **Option 1 (클라이언트 측 확장)**

---

### 📊 데이터 모델

#### Event 타입 (기존)

```typescript
export interface Event extends EventForm {
  id: string;
}
```

#### EventForm 타입 (기존)

```typescript
export interface EventForm {
  title: string;
  date: string; // 기준 날짜 (YYYY-MM-DD)
  startTime: string; // HH:mm
  endTime: string; // HH:mm
  description: string;
  location: string;
  category: string;
  repeat: RepeatInfo;
  notificationTime: number; // 분 단위
}
```

#### RepeatInfo 타입 (기존)

```typescript
export interface RepeatInfo {
  type: RepeatType; // 'none' | 'daily' | 'weekly' | 'monthly' | 'yearly'
  interval: number; // 반복 간격 (1 = 매일/매주/매월/매년)
  endDate?: string; // 반복 종료일 (YYYY-MM-DD, 최대 2025-12-31)
}
```

#### RecurringEventInstance 타입 (신규, 클라이언트 전용)

```typescript
// Ai Edit
export interface RecurringEventInstance extends Event {
  isRecurring: boolean; // true로 고정
  instanceDate: string; // 실제 표시 날짜 (YYYY-MM-DD)
  originalEventId: string; // 원본 이벤트 ID (기준 이벤트)
}
```

---

### 🔄 반복 일정 확장 로직

#### 핵심 함수: `expandRecurringEvents()`

```typescript
// Ai Edit
/**
 * 🔄 반복 일정을 개별 이벤트 인스턴스로 확장
 * @param events - 원본 이벤트 목록
 * @param startDate - 확장 시작 날짜 (뷰 범위 시작)
 * @param endDate - 확장 종료 날짜 (뷰 범위 끝)
 * @returns 확장된 이벤트 인스턴스 목록
 */
export function expandRecurringEvents(
  events: Event[],
  startDate: Date,
  endDate: Date
): RecurringEventInstance[] {
  const expandedEvents: RecurringEventInstance[] = [];

  for (const event of events) {
    if (event.repeat.type === 'none') {
      // 단일 일정은 그대로 추가
      expandedEvents.push(event as RecurringEventInstance);
      continue;
    }

    // 반복 일정 확장
    const recurringDates = generateRecurringDates(
      new Date(event.date),
      event.repeat.type,
      event.repeat.interval,
      event.repeat.endDate ? new Date(event.repeat.endDate) : new Date('2025-12-31'),
      startDate,
      endDate
    );

    for (const instanceDate of recurringDates) {
      expandedEvents.push(createRecurringInstance(event, instanceDate));
    }
  }

  return expandedEvents;
}
```

#### 핵심 함수: `generateRecurringDates()`

```typescript
// Ai Edit
/**
 * 📅 반복 날짜 생성
 * @param startDate - 기준 날짜
 * @param repeatType - 반복 유형
 * @param interval - 반복 간격
 * @param endDate - 반복 종료일
 * @param viewStart - 뷰 시작 날짜 (필터링용)
 * @param viewEnd - 뷰 종료 날짜 (필터링용)
 * @returns 반복 날짜 배열
 */
export function generateRecurringDates(
  startDate: Date,
  repeatType: RepeatType,
  interval: number,
  endDate: Date,
  viewStart: Date,
  viewEnd: Date
): Date[] {
  const dates: Date[] = [];
  let currentDate = new Date(startDate);
  const maxDate = endDate > new Date('2025-12-31') ? new Date('2025-12-31') : endDate;

  while (currentDate <= maxDate) {
    // 뷰 범위 내에 있는 날짜만 추가
    if (currentDate >= viewStart && currentDate <= viewEnd) {
      // 특수 케이스 유효성 검증
      if (isValidRecurringDate(currentDate, startDate, repeatType)) {
        dates.push(new Date(currentDate));
      }
    }

    // 다음 반복 날짜 계산
    currentDate = calculateNextRecurringDate(currentDate, repeatType, interval);
  }

  return dates;
}
```

#### 특수 케이스 처리: `isValidRecurringDate()`

```typescript
// Ai Edit
/**
 * 🔍 반복 날짜 유효성 검증 (특수 케이스 처리)
 * @param date - 검증할 날짜
 * @param originalDate - 원본 기준 날짜
 * @param repeatType - 반복 유형
 * @returns 유효 여부
 */
export function isValidRecurringDate(
  date: Date,
  originalDate: Date,
  repeatType: RepeatType
): boolean {
  const originalDay = originalDate.getDate();

  if (repeatType === 'monthly') {
    // 31일 특수 케이스: 31일이 없는 달은 제외
    if (originalDay === 31) {
      return date.getDate() === 31;
    }
  }

  if (repeatType === 'yearly') {
    // 윤년 2월 29일 특수 케이스: 윤년만 포함
    if (originalDate.getMonth() === 1 && originalDay === 29) {
      return date.getMonth() === 1 && date.getDate() === 29 && isLeapYear(date.getFullYear());
    }
  }

  return true;
}
```

---

## 🛡️ 아키텍처 가드레일 (Guardrails)

### 🔒 구조적 변경 원칙

- [x] **기존 타입 수정 금지**: `Event`, `EventForm`, `RepeatInfo` 타입은 수정하지 않음
  - ✅ 타입 확장은 허용 (예: `RecurringEventInstance extends Event`)
- [x] **신규 파일 생성 금지**: 기존 파일에 함수 추가 방식으로 구현
  - ✅ `dateUtils.ts`, `eventUtils.ts`에 신규 함수 추가
- [x] **API 인터페이스 변경 최소화**: 서버 API는 변경하지 않음
  - ✅ 클라이언트 측 확장 로직으로 해결
- [x] **컴포넌트 구조 유지**: `App.tsx`의 전체 구조는 변경하지 않음
  - ✅ 주석 처리된 UI 활성화 및 다이얼로그 추가만 수행

---

### ⚙️ 행동적 변경 원칙

- [x] **TDD 준수**: 모든 함수는 테스트를 먼저 작성
  - ✅ 유닛 테스트: `generateRecurringDates`, `isValidRecurringDate`, `expandRecurringEvents`
  - ✅ 통합 테스트: `useEventOperations`, `useCalendarView`
- [x] **Pure Function 원칙**: Utils 함수는 부수 효과 없이 순수 함수로 작성
  - ✅ 입력 파라미터 수정하지 않음
  - ✅ 예측 가능한 출력
- [x] **에러 처리**: 모든 함수는 유효성 검증 및 에러 처리 포함
  - ✅ 날짜 유효성 검증
  - ✅ 반복 종료일 제한 (2025-12-31)
  - ✅ 사용자 친화적 에러 메시지
- [x] **성능 최적화**: 불필요한 연산 최소화
  - ✅ 뷰 범위만 확장
  - ✅ 메모이제이션 활용 (useMemo, useCallback)
- [x] **타입 안정성**: TypeScript strict 모드 준수
  - ✅ any 타입 사용 금지
  - ✅ 모든 함수와 변수에 명시적 타입 지정

---

### 🔐 호환성 원칙

- [x] **하위 호환성**: 기존 단일 일정 기능은 그대로 유지
  - ✅ `repeat.type === 'none'`인 경우 기존 로직 사용
- [x] **점진적 구현**: Phase별 독립적 구현 가능
  - ✅ Phase 1 완료 후 Phase 2 진행 가능
- [x] **롤백 가능성**: 각 Phase는 독립적으로 롤백 가능
  - ✅ 각 Phase별 커밋 분리

---

## 📊 기술 스택

### 🎨 프론트엔드

- **프레임워크**: React 19.1.0
- **언어**: TypeScript 5.2.2
- **UI 라이브러리**: Material-UI (MUI) 7.2.0
- **아이콘**: @mui/icons-material/Repeat
- **상태 관리**: React Hooks (useState, useEffect, useMemo, useCallback)
- **알림**: Notistack 3.0.2

### 🔧 개발 도구

- **빌드 도구**: Vite 7.0.2
- **테스트 프레임워크**: Vitest 3.2.4
- **테스트 라이브러리**:
  - React Testing Library 16.3.0
  - @testing-library/user-event 14.5.2
  - @testing-library/jest-dom 6.6.3
- **Linter**: ESLint 9.30.0
- **타입 체킹**: TypeScript (strict 모드)

### 🗄️ 백엔드

- **서버**: Express.js 4.19.2
- **Mock 데이터**: JSON 파일
- **API 스타일**: RESTful API

### 🏗️ 아키텍처 패턴

- **UI 패턴**: Presentational/Container 분리
- **상태 관리**: Custom Hooks 패턴
- **비즈니스 로직**: Pure Function Utils
- **테스트**: TDD (Test-Driven Development)

---

## 🔄 데이터 플로우

### 📥 반복 일정 생성 플로우

```
1. 사용자 입력
   ↓
2. useEventForm (폼 상태 수집)
   - title, date, time, repeat settings 등
   ↓
3. useEventOperations.saveEvent()
   ↓
4. POST /api/events (서버에 기준 이벤트 저장)
   ↓
5. 서버 응답 (저장된 이벤트)
   ↓
6. useEventOperations.fetchEvents() (전체 이벤트 재로드)
   ↓
7. events 상태 업데이트
   ↓
8. App.tsx 렌더링
   ↓
9. expandRecurringEvents() 호출 (뷰 범위 확장)
   ↓
10. 캘린더 뷰 및 일정 목록 렌더링
```

---

### 📝 반복 일정 수정 플로우 (단일)

```
1. 사용자가 반복 일정 수정 클릭
   ↓
2. "해당 일정만 수정하시겠어요?" 다이얼로그 표시
   ↓
3. 사용자가 "예" 클릭
   ↓
4. useEventForm에서 폼 데이터 수집
   ↓
5. repeat.type을 'none'으로 변경
   ↓
6. useEventOperations.saveEvent() (단일 일정으로 저장)
   ↓
7. PUT /api/events/:id
   ↓
8. 서버 응답
   ↓
9. fetchEvents() (재로드)
   ↓
10. 반복 아이콘 사라짐
```

---

### 📝 반복 일정 수정 플로우 (전체)

```
1. 사용자가 반복 일정 수정 클릭
   ↓
2. "해당 일정만 수정하시겠어요?" 다이얼로그 표시
   ↓
3. 사용자가 "아니오" 클릭
   ↓
4. useEventForm에서 폼 데이터 수집
   ↓
5. repeat 속성 유지
   ↓
6. useEventOperations.saveEvent() (기준 이벤트 수정)
   ↓
7. PUT /api/events/:id (원본 이벤트 ID로 수정)
   ↓
8. 서버 응답
   ↓
9. fetchEvents() (재로드)
   ↓
10. 모든 반복 일정에 변경 사항 반영
```

---

### 🗑️ 반복 일정 삭제 플로우 (단일)

```
1. 사용자가 반복 일정 삭제 클릭
   ↓
2. "해당 일정만 삭제하시겠어요?" 다이얼로그 표시
   ↓
3. 사용자가 "예" 클릭
   ↓
4. 해당 날짜의 인스턴스만 삭제 처리
   ↓
5. Option A: 예외 날짜 목록(exceptions) 업데이트
   PUT /api/events/:id (exceptions 필드 추가)
   ↓
6. Option B: 단일 이벤트로 분리 후 삭제
   (현재 과제에서는 Option A 권장)
   ↓
7. fetchEvents() (재로드)
   ↓
8. 해당 날짜에만 일정 미표시
```

---

### 🗑️ 반복 일정 삭제 플로우 (전체)

```
1. 사용자가 반복 일정 삭제 클릭
   ↓
2. "해당 일정만 삭제하시겠어요?" 다이얼로그 표시
   ↓
3. 사용자가 "아니오" 클릭
   ↓
4. useEventOperations.deleteEvent(originalEventId)
   ↓
5. DELETE /api/events/:id (원본 이벤트 ID)
   ↓
6. 서버 응답
   ↓
7. fetchEvents() (재로드)
   ↓
8. 모든 반복 일정이 캘린더에서 사라짐
```

---

## 🚨 에러 처리 전략

### ⚠️ 에러 분류

#### 1️⃣ 클라이언트 에러 (4xx)

**입력 유효성 에러**

- **발생 시점**: 폼 입력 시
- **처리 방법**:
  - 프론트엔드에서 사전 검증
  - 에러 메시지 툴팁 표시
  - 제출 버튼 비활성화
- **예시**:
  - "제목은 필수입니다"
  - "시작 시간은 종료 시간보다 빨라야 합니다"
  - "반복 종료일은 2025-12-31까지만 설정 가능합니다"

**날짜 유효성 에러**

- **발생 시점**: 반복 날짜 생성 시
- **처리 방법**:
  - `isValidRecurringDate()` 함수로 검증
  - 유효하지 않은 날짜는 건너뜀
- **예시**:
  - 31일이 없는 달 건너뛰기
  - 윤년이 아닌 해의 2월 29일 건너뛰기

---

#### 2️⃣ 서버 에러 (5xx)

**API 요청 실패**

- **발생 시점**: 이벤트 CRUD 작업 시
- **처리 방법**:
  - try-catch로 에러 포착
  - Notistack으로 사용자에게 알림
  - 콘솔에 에러 로깅
- **예시**:
  - "일정 저장 실패"
  - "일정 삭제 실패"
  - "일정 로딩 실패"

---

### 📋 로깅 전략

#### 개발 환경

```typescript
// Ai Edit
console.error('이벤트 로딩 중 오류 발생:', error);
console.warn('특수 날짜 건너뛰기:', date);
console.info('반복 일정 확장 완료:', expandedEvents.length);
```

#### 프로덕션 환경

```typescript
// Ai Edit
// 민감 정보 제외하고 로깅
if (process.env.NODE_ENV === 'production') {
  // Sentry, LogRocket 등 외부 모니터링 도구 활용
}
```

---

### 🔍 모니터링

#### 성능 모니터링

- 반복 일정 확장 시간 측정
- 캘린더 렌더링 시간 측정
- 대량 이벤트 처리 성능 추적

#### 에러 모니터링

- API 요청 실패율
- 클라이언트 에러 발생 빈도
- 특수 케이스 처리 실패율

---

## 🔄 다음 단계

### 📌 Scrum Master 에이전트

- [x] Phase별 Story 파일 생성
  - Story 1: 반복 일정 생성
  - Story 2: 캘린더 뷰 표시
  - Story 3: 반복 일정 수정
  - Story 4: 반복 일정 삭제
  - Story 5: 테스트 및 QA
- [x] 각 Story의 구현 지시사항 및 테스트 힌트 작성
- [x] 수용 기준 및 완료 조건 명확히 정의

### 📌 Dev 에이전트

- [x] 아키텍처 가이드라인 준수
- [x] TDD 방식으로 테스트 먼저 작성
- [x] Pure Function 원칙 준수
- [x] 타입 안정성 100% 유지
- [x] Phase별 순차 구현

---

## ✅ Architect 체크리스트

- [x] 모든 컴포넌트의 역할이 명확함
- [x] 인터페이스가 정의됨 (API, 데이터 모델)
- [x] 가드레일이 설정됨 (구조적/행동적/호환성 원칙)
- [x] 기술 스택이 결정됨
- [x] 데이터 플로우가 명확함 (4가지 플로우 상세 정의)
- [x] 에러 처리 전략이 수립됨
- [x] 다음 에이전트가 구현할 수 있는 충분한 정보 제공

---

## 📈 점수 현황 (Score Status)

- **획득 점수 (Acquired Score):** 7점 (체크리스트 7개 항목 완료)
- **누적 점수 (Cumulative Score):** 32점 (이전 25점 + 현재 7점)
- **총점 (Total Score):** 예상 총점 100점

---

**작성자**: BMAD Architect  
**다음 핸드오프**: Scrum Master → Dev → QA  
**참조 문서**:

- `mockdowns/artifacts/orchestrator/2025-10-28_PRD_summary_v1.0.md`
- `mockdowns/artifacts/orchestrator/2025-10-28_Architecture_summary_v1.0.md`
- `mockdowns/artifacts/analyst/2025-10-28_반복일정기능_PRD_v1.0.md`
- `mockdowns/artifacts/pm/2025-10-28_반복일정기능_로드맵_v1.0.md`
- `mockdowns/artifacts/2025-10-28_project_structure_v1.0.md`
