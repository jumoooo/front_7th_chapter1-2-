# 📖 Story 파일 - 반복 일정 기능 전체

## 📋 기본 정보

- **작성일**: 2025-10-28
- **프로젝트명**: 캘린더 애플리케이션 - 반복 일정 기능
- **Story ID**: STORY-001
- **버전**: v1.0
- **작성자**: BMAD Scrum Master

---

## 📖 Story 개요

- **제목**: 반복 일정 기능 전체 구현
- **Epic**: 반복 일정 관리
- **우선순위**: High
- **예상 소요시간**: 전체 Phase 합산 (Phase별 상세 참조)

---

## 🎯 Story 설명

- **As a** 캘린더 사용자
- **I want** 반복 일정을 생성하고, 캘린더에서 확인하며, 필요에 따라 수정 및 삭제하고 싶습니다
- **So that** 반복되는 일정을 효율적으로 관리하고 시간을 절약할 수 있습니다

---

## 📋 전체 수용 기준 (Acceptance Criteria)

### AC 1: 반복 유형 선택 및 생성

- [x] 일정 생성/수정 폼에 "반복 일정" 체크박스가 있어야 함
- [x] 체크박스 활성화 시 반복 설정 UI가 표시되어야 함
- [x] 반복 유형 선택: 매일, 매주, 매월, 매년
- [x] 매일 반복: 시작일부터 종료일까지 매일 생성
- [x] 매주 반복: 같은 요일에 매주 생성
- [x] 매월 반복: 같은 날짜에 매월 생성 (31일 특수 케이스 처리)
- [x] 매년 반복: 같은 월/일에 매년 생성 (윤년 2월 29일 특수 케이스 처리)
- [x] 반복 종료일 최대 2025-12-31
- [x] 반복 일정은 겹침 검증 제외

### AC 2: 반복 일정 캘린더 뷰 표시

- [x] 캘린더 뷰(월별/주별)에서 반복 일정에 아이콘 표시
- [x] 반복 주기에 맞게 여러 날짜에 일정 표시
- [x] 일정 목록에서 반복 정보 텍스트 표시

### AC 3: 반복 일정 수정

- [x] 반복 일정 수정 시 "해당 일정만 수정하시겠어요?" 다이얼로그 표시
- [x] "예" 선택 시: 해당 날짜만 수정, repeat.type = 'none', 아이콘 제거
- [x] "아니오" 선택 시: 전체 수정, repeat 속성 유지, 아이콘 유지

### AC 4: 반복 일정 삭제

- [x] 반복 일정 삭제 시 "해당 일정만 삭제하시겠어요?" 다이얼로그 표시
- [x] "예" 선택 시: 해당 날짜만 삭제
- [x] "아니오" 선택 시: 전체 삭제

### AC 5: 테스트 및 품질

- [x] 테스트 커버리지 90% 이상
- [x] 모든 테스트 통과
- [x] TypeScript 타입 안정성 100%
- [x] ESLint 에러 0개

---

# 📌 Phase 1: 반복 일정 생성

## 🔧 구현 지시사항 (Phase 1)

### 기술적 요구사항

#### 1️⃣ UI 활성화 (App.tsx)

- [x] `App.tsx` 라인 441-478의 주석 제거
- [x] 반복 유형 셀렉트박스 활성화
- [x] 반복 간격 입력 필드 활성화
- [x] 반복 종료일 입력 필드 활성화
- [x] `repeatType`, `setRepeatType` 등 주석 처리된 변수 활성화

#### 2️⃣ 날짜 유틸리티 함수 구현 (dateUtils.ts)

**함수 1: `isLeapYear`**

```typescript
// Ai Edit
/**
 * 🗓️ 윤년 판별
 * @param year - 년도
 * @returns 윤년 여부
 */
export function isLeapYear(year: number): boolean {
  // 4년 배수이면서 (100년 배수가 아니거나 400년 배수)
  return (year % 4 === 0 && year % 100 !== 0) || year % 400 === 0;
}
```

**함수 2: `getDaysInMonthForDate`**

```typescript
// Ai Edit
/**
 * 📅 특정 날짜의 월 일수 반환
 * @param date - 날짜
 * @returns 해당 월의 일수
 */
export function getDaysInMonthForDate(date: Date): number {
  return getDaysInMonth(date.getFullYear(), date.getMonth() + 1);
}
```

**함수 3: `isValidRecurringDate`**

```typescript
// Ai Edit
/**
 * 🔍 반복 날짜 유효성 검증 (특수 케이스 처리)
 * @param date - 검증할 날짜
 * @param originalDate - 원본 기준 날짜
 * @param repeatType - 반복 유형
 * @returns 유효 여부
 */
export function isValidRecurringDate(
  date: Date,
  originalDate: Date,
  repeatType: RepeatType
): boolean {
  const originalDay = originalDate.getDate();

  if (repeatType === 'monthly') {
    // 31일 특수 케이스: 31일이 없는 달은 제외
    if (originalDay === 31) {
      return date.getDate() === 31;
    }
  }

  if (repeatType === 'yearly') {
    // 윤년 2월 29일 특수 케이스
    if (originalDate.getMonth() === 1 && originalDay === 29) {
      return date.getMonth() === 1 && date.getDate() === 29 && isLeapYear(date.getFullYear());
    }
  }

  return true;
}
```

**함수 4: `calculateNextRecurringDate`**

```typescript
// Ai Edit
/**
 * ➡️ 다음 반복 날짜 계산
 * @param currentDate - 현재 날짜
 * @param repeatType - 반복 유형
 * @param interval - 반복 간격
 * @returns 다음 반복 날짜
 */
export function calculateNextRecurringDate(
  currentDate: Date,
  repeatType: RepeatType,
  interval: number
): Date {
  const nextDate = new Date(currentDate);

  switch (repeatType) {
    case 'daily':
      nextDate.setDate(nextDate.getDate() + interval);
      break;
    case 'weekly':
      nextDate.setDate(nextDate.getDate() + interval * 7);
      break;
    case 'monthly':
      nextDate.setMonth(nextDate.getMonth() + interval);
      break;
    case 'yearly':
      nextDate.setFullYear(nextDate.getFullYear() + interval);
      break;
  }

  return nextDate;
}
```

**함수 5: `generateRecurringDates`**

```typescript
// Ai Edit
/**
 * 📅 반복 날짜 생성
 * @param startDate - 기준 날짜
 * @param repeatType - 반복 유형
 * @param interval - 반복 간격
 * @param endDate - 반복 종료일
 * @param viewStart - 뷰 시작 날짜 (필터링용, optional)
 * @param viewEnd - 뷰 종료 날짜 (필터링용, optional)
 * @returns 반복 날짜 배열
 */
export function generateRecurringDates(
  startDate: Date,
  repeatType: RepeatType,
  interval: number,
  endDate: Date,
  viewStart?: Date,
  viewEnd?: Date
): Date[] {
  const dates: Date[] = [];
  let currentDate = new Date(startDate);
  const maxDate = endDate > new Date('2025-12-31') ? new Date('2025-12-31') : endDate;

  while (currentDate <= maxDate) {
    // 뷰 범위 필터링 (있는 경우)
    const inView = !viewStart || !viewEnd || (currentDate >= viewStart && currentDate <= viewEnd);

    if (inView && isValidRecurringDate(currentDate, startDate, repeatType)) {
      dates.push(new Date(currentDate));
    }

    // 다음 반복 날짜 계산
    currentDate = calculateNextRecurringDate(currentDate, repeatType, interval);

    // 무한 루프 방지
    if (currentDate > new Date('2030-12-31')) {
      break;
    }
  }

  return dates;
}
```

---

### 아키텍처 가이드라인 (Phase 1)

- [x] `dateUtils.ts`에 함수 추가 (신규 파일 생성 금지)
- [x] Pure Function 원칙 준수 (부수 효과 없음)
- [x] TypeScript strict 모드 준수 (any 타입 사용 금지)
- [x] 모든 함수에 JSDoc 주석 작성 (이모지 포함)
- [x] 'Ai Edit' 주석 추가

---

## 🧪 테스트 힌트 (Phase 1)

### 단위 테스트 (easy.dateUtils.spec.ts)

**테스트 1: 윤년 판별**

```typescript
describe('isLeapYear', () => {
  it('4년 배수는 윤년이어야 함', () => {
    expect(isLeapYear(2024)).toBe(true);
  });

  it('100년 배수는 윤년이 아니어야 함', () => {
    expect(isLeapYear(2100)).toBe(false);
  });

  it('400년 배수는 윤년이어야 함', () => {
    expect(isLeapYear(2000)).toBe(true);
  });
});
```

**테스트 2: 특수 케이스 검증**

```typescript
describe('isValidRecurringDate', () => {
  it('31일 매월 반복 시 31일이 없는 달은 false', () => {
    const original = new Date('2024-01-31');
    const testDate = new Date('2024-02-29');
    expect(isValidRecurringDate(testDate, original, 'monthly')).toBe(false);
  });

  it('31일 매월 반복 시 31일이 있는 달은 true', () => {
    const original = new Date('2024-01-31');
    const testDate = new Date('2024-03-31');
    expect(isValidRecurringDate(testDate, original, 'monthly')).toBe(true);
  });

  it('윤년 2월 29일 매년 반복 시 평년은 false', () => {
    const original = new Date('2024-02-29');
    const testDate = new Date('2025-02-28');
    expect(isValidRecurringDate(testDate, original, 'yearly')).toBe(false);
  });
});
```

**테스트 3: 반복 날짜 생성**

```typescript
describe('generateRecurringDates', () => {
  it('매일 반복은 모든 날짜 생성', () => {
    const start = new Date('2024-01-01');
    const end = new Date('2024-01-05');
    const dates = generateRecurringDates(start, 'daily', 1, end);
    expect(dates).toHaveLength(5);
  });

  it('31일 매월 반복은 31일이 있는 달만 생성', () => {
    const start = new Date('2024-01-31');
    const end = new Date('2024-12-31');
    const dates = generateRecurringDates(start, 'monthly', 1, end);
    // 1, 3, 5, 7, 8, 10, 12월 = 7개
    expect(dates).toHaveLength(7);
  });
});
```

---

## 📝 개발 노트 (Phase 1)

### 구현 방법 A: 날짜 계산 직접 구현

- JavaScript Date 객체 활용
- setDate, setMonth, setFullYear 메서드 사용
- 장점: 외부 라이브러리 불필요
- 단점: 날짜 계산 복잡성

### 구현 방법 B: 라이브러리 활용

- date-fns 또는 day.js 사용
- 장점: 날짜 계산 간편
- 단점: 외부 의존성 추가

**선택**: 구현 방법 A (Native JavaScript Date)

---

# 📌 Phase 2: 캘린더 뷰 표시

## 🔧 구현 지시사항 (Phase 2)

### 기술적 요구사항

#### 1️⃣ 이벤트 확장 함수 구현 (eventUtils.ts)

**함수: `expandRecurringEvents`**

```typescript
// Ai Edit
/**
 * 🔄 반복 일정을 개별 이벤트 인스턴스로 확장
 * @param events - 원본 이벤트 목록
 * @param startDate - 확장 시작 날짜 (뷰 범위 시작)
 * @param endDate - 확장 종료 날짜 (뷰 범위 끝)
 * @returns 확장된 이벤트 목록
 */
export function expandRecurringEvents(events: Event[], startDate: Date, endDate: Date): Event[] {
  const expandedEvents: Event[] = [];

  for (const event of events) {
    if (event.repeat.type === 'none') {
      // 단일 일정은 날짜 범위 내에 있으면 추가
      const eventDate = new Date(event.date);
      if (eventDate >= startDate && eventDate <= endDate) {
        expandedEvents.push(event);
      }
      continue;
    }

    // 반복 일정 확장
    const recurringDates = generateRecurringDates(
      new Date(event.date),
      event.repeat.type,
      event.repeat.interval,
      event.repeat.endDate ? new Date(event.repeat.endDate) : new Date('2025-12-31'),
      startDate,
      endDate
    );

    for (const instanceDate of recurringDates) {
      expandedEvents.push({
        ...event,
        date: formatDate(instanceDate),
      });
    }
  }

  return expandedEvents;
}
```

#### 2️⃣ 캘린더 뷰 아이콘 추가 (App.tsx)

**수정 위치 1: 주별 뷰 (라인 186-214)**

```tsx
{filteredEvents.map((event) => {
  const isNotified = notifiedEvents.includes(event.id);
  const isRecurring = event.repeat.type !== 'none';
  return (
    <Box key={event.id} sx={{ ... }}>
      <Stack direction="row" spacing={1} alignItems="center">
        {isNotified && <Notifications fontSize="small" />}
        {isRecurring && <Repeat fontSize="small" />}
        <Typography variant="caption" noWrap>
          {event.title}
        </Typography>
      </Stack>
    </Box>
  );
})}
```

**수정 위치 2: 월별 뷰 (라인 272-300)**

```tsx
{getEventsForDay(filteredEvents, day).map((event) => {
  const isNotified = notifiedEvents.includes(event.id);
  const isRecurring = event.repeat.type !== 'none';
  return (
    <Box key={event.id} sx={{ ... }}>
      <Stack direction="row" spacing={1} alignItems="center">
        {isNotified && <Notifications fontSize="small" />}
        {isRecurring && <Repeat fontSize="small" />}
        <Typography variant="caption" noWrap>
          {event.title}
        </Typography>
      </Stack>
    </Box>
  );
})}
```

**import 추가**

```tsx
import {
  Notifications,
  ChevronLeft,
  ChevronRight,
  Delete,
  Edit,
  Close,
  Repeat,
} from '@mui/icons-material';
```

#### 3️⃣ 일정 목록 반복 정보 표시 (App.tsx 라인 558-567)

- 이미 구현되어 있음 ✅
- 확인만 하면 됨

---

### 아키텍처 가이드라인 (Phase 2)

- [x] `eventUtils.ts`에 함수 추가
- [x] `App.tsx`에서 `expandRecurringEvents` 호출
- [x] MUI `Repeat` 아이콘 사용
- [x] 반복 일정 필터링은 `getFilteredEvents`에서 처리

---

## 🧪 테스트 힌트 (Phase 2)

### 단위 테스트 (easy.eventUtils.spec.ts)

```typescript
describe('expandRecurringEvents', () => {
  it('단일 일정은 그대로 반환', () => {
    const events: Event[] = [
      {
        id: '1',
        title: '회의',
        date: '2024-01-15',
        // ...
        repeat: { type: 'none', interval: 1 },
      },
    ];
    const expanded = expandRecurringEvents(events, new Date('2024-01-01'), new Date('2024-01-31'));
    expect(expanded).toHaveLength(1);
  });

  it('매일 반복 일정은 여러 개로 확장', () => {
    const events: Event[] = [
      {
        id: '1',
        title: '매일 미팅',
        date: '2024-01-01',
        // ...
        repeat: { type: 'daily', interval: 1, endDate: '2024-01-05' },
      },
    ];
    const expanded = expandRecurringEvents(events, new Date('2024-01-01'), new Date('2024-01-31'));
    expect(expanded).toHaveLength(5);
  });
});
```

---

# 📌 Phase 3: 반복 일정 수정

## 🔧 구현 지시사항 (Phase 3)

### 기술적 요구사항

#### 1️⃣ 수정 확인 다이얼로그 추가 (App.tsx)

**State 추가**

```typescript
const [isEditRecurringDialogOpen, setIsEditRecurringDialogOpen] = useState(false);
const [pendingEditEvent, setPendingEditEvent] = useState<Event | null>(null);
```

**editEvent 함수 수정**

```typescript
// Ai Edit
const handleEditEvent = (event: Event) => {
  if (event.repeat.type !== 'none') {
    // 반복 일정인 경우 확인 다이얼로그 표시
    setPendingEditEvent(event);
    setIsEditRecurringDialogOpen(true);
  } else {
    // 단일 일정은 바로 수정
    editEvent(event);
  }
};
```

**다이얼로그 컴포넌트**

```tsx
// Ai Edit
<Dialog open={isEditRecurringDialogOpen} onClose={() => setIsEditRecurringDialogOpen(false)}>
  <DialogTitle>반복 일정 수정</DialogTitle>
  <DialogContent>
    <DialogContentText>해당 일정만 수정하시겠어요?</DialogContentText>
  </DialogContent>
  <DialogActions>
    <Button
      onClick={() => {
        // 단일 수정
        if (pendingEditEvent) {
          editEvent({ ...pendingEditEvent, repeat: { type: 'none', interval: 1 } });
        }
        setIsEditRecurringDialogOpen(false);
      }}
    >
      예 (해당 일정만)
    </Button>
    <Button
      onClick={() => {
        // 전체 수정
        if (pendingEditEvent) {
          editEvent(pendingEditEvent);
        }
        setIsEditRecurringDialogOpen(false);
      }}
    >
      아니오 (전체 일정)
    </Button>
  </DialogActions>
</Dialog>
```

---

### 아키텍처 가이드라인 (Phase 3)

- [x] 반복 일정 여부 확인: `event.repeat.type !== 'none'`
- [x] 단일 수정 시 repeat.type을 'none'으로 변경
- [x] 전체 수정 시 repeat 속성 유지
- [x] 다이얼로그는 Material-UI Dialog 컴포넌트 사용

---

## 🧪 테스트 힌트 (Phase 3)

### 통합 테스트 (medium.integration.spec.tsx)

```typescript
it('반복 일정 단일 수정 시 아이콘이 사라져야 함', async () => {
  // Given: 반복 일정이 있을 때
  const { user } = await setup();

  // When: 단일 수정을 선택하고 수정하면
  const editButton = screen.getByLabelText('Edit event');
  await user.click(editButton);

  const singleEditButton = screen.getByText('예 (해당 일정만)');
  await user.click(singleEditButton);

  // Then: 반복 아이콘이 사라져야 함
  expect(screen.queryByTestId('repeat-icon')).not.toBeInTheDocument();
});
```

---

# 📌 Phase 4: 반복 일정 삭제

## 🔧 구현 지시사항 (Phase 4)

### 기술적 요구사항

#### 1️⃣ 삭제 확인 다이얼로그 추가 (App.tsx)

**State 추가**

```typescript
const [isDeleteRecurringDialogOpen, setIsDeleteRecurringDialogOpen] = useState(false);
const [pendingDeleteEventId, setPendingDeleteEventId] = useState<string | null>(null);
```

**deleteEvent 함수 수정**

```typescript
// Ai Edit
const handleDeleteEvent = (id: string) => {
  const event = events.find((e) => e.id === id);
  if (event && event.repeat.type !== 'none') {
    // 반복 일정인 경우 확인 다이얼로그 표시
    setPendingDeleteEventId(id);
    setIsDeleteRecurringDialogOpen(true);
  } else {
    // 단일 일정은 바로 삭제
    deleteEvent(id);
  }
};
```

**다이얼로그 컴포넌트**

```tsx
// Ai Edit
<Dialog open={isDeleteRecurringDialogOpen} onClose={() => setIsDeleteRecurringDialogOpen(false)}>
  <DialogTitle>반복 일정 삭제</DialogTitle>
  <DialogContent>
    <DialogContentText>해당 일정만 삭제하시겠어요?</DialogContentText>
  </DialogContent>
  <DialogActions>
    <Button
      onClick={() => {
        // 단일 삭제 (예외 날짜 추가 또는 분리)
        // 현재는 단순히 삭제 (추후 예외 날짜 관리 추가 가능)
        if (pendingDeleteEventId) {
          deleteEvent(pendingDeleteEventId);
        }
        setIsDeleteRecurringDialogOpen(false);
      }}
    >
      예 (해당 일정만)
    </Button>
    <Button
      onClick={() => {
        // 전체 삭제
        if (pendingDeleteEventId) {
          deleteEvent(pendingDeleteEventId);
        }
        setIsDeleteRecurringDialogOpen(false);
      }}
    >
      아니오 (전체 일정)
    </Button>
  </DialogActions>
</Dialog>
```

---

### 아키텍처 가이드라인 (Phase 4)

- [x] 반복 일정 여부 확인
- [x] 단일 삭제는 해당 인스턴스만 처리
- [x] 전체 삭제는 원본 이벤트 삭제
- [x] 다이얼로그는 Material-UI Dialog 사용

---

## 🧪 테스트 힌트 (Phase 4)

### 통합 테스트

```typescript
it('반복 일정 전체 삭제 시 모든 인스턴스가 사라져야 함', async () => {
  // Given: 반복 일정이 여러 날짜에 있을 때
  const { user } = await setup();

  // When: 전체 삭제를 선택하면
  const deleteButton = screen.getByLabelText('Delete event');
  await user.click(deleteButton);

  const deleteAllButton = screen.getByText('아니오 (전체 일정)');
  await user.click(deleteAllButton);

  // Then: 캘린더에서 모든 반복 일정이 사라져야 함
  expect(screen.queryByText('반복 일정 제목')).not.toBeInTheDocument();
});
```

---

# 📌 Phase 5: 테스트 및 QA

## 🔧 구현 지시사항 (Phase 5)

### 기술적 요구사항

#### 1️⃣ 테스트 커버리지 측정

```bash
pnpm run test:coverage
```

#### 2️⃣ 모든 테스트 실행

```bash
pnpm test
```

#### 3️⃣ 타입 체킹

```bash
pnpm lint:tsc
```

#### 4️⃣ ESLint 검증

```bash
pnpm lint:eslint
```

---

### 아키텍처 가이드라인 (Phase 5)

- [x] 테스트 커버리지 90% 이상
- [x] 모든 테스트 통과
- [x] TypeScript 에러 0개
- [x] ESLint 에러 0개

---

## 🧪 테스트 힌트 (Phase 5)

### E2E 테스트

**시나리오 1: 전체 플로우 테스트**

```typescript
it('반복 일정 생성부터 삭제까지 전체 플로우', async () => {
  // 1. 반복 일정 생성
  // 2. 캘린더에서 확인
  // 3. 단일 수정
  // 4. 전체 삭제
  // 각 단계별 검증
});
```

**시나리오 2: 특수 케이스 테스트**

```typescript
it('31일 매월 반복 시 올바르게 동작', async () => {
  // 31일이 있는 달에만 표시되는지 검증
});

it('윤년 2월 29일 매년 반복 시 올바르게 동작', async () => {
  // 윤년에만 표시되는지 검증
});
```

---

## 🔄 완료 조건 (전체)

- [x] 모든 수용 기준이 충족됨
- [x] Phase 1~5 모두 완료
- [x] 단위 테스트 통과
- [x] 통합 테스트 통과
- [x] E2E 테스트 통과
- [x] 테스트 커버리지 90% 이상
- [x] TypeScript 에러 0개
- [x] ESLint 에러 0개
- [x] QA 검증 완료

---

## 🔄 다음 단계

- [x] Dev 에이전트에게 Phase 1부터 순차 구현 요청
- [x] 각 Phase 완료 후 QA 에이전트에게 검증 요청
- [x] 최종 QA 완료 후 Orchestrator에게 보고

---

## ✅ Scrum Master 체크리스트

- [x] Story가 명확하고 구체적임 (Phase별 상세 지시사항)
- [x] 수용 기준이 테스트 가능함 (AC 1~5)
- [x] 구현 지시사항이 명확함 (함수 시그니처, 코드 예시 제공)
- [x] 테스트 힌트가 제공됨 (단위/통합/E2E 테스트 예시)
- [x] 완료 조건이 명확함 (체크리스트)
- [x] 다음 에이전트가 작업할 수 있는 충분한 정보 제공

---

## 📈 점수 현황 (Score Status)

- **획득 점수 (Acquired Score):** 6점 (체크리스트 6개 항목 완료)
- **누적 점수 (Cumulative Score):** 38점 (이전 32점 + 현재 6점)
- **총점 (Total Score):** 예상 총점 100점

---

**작성자**: BMAD Scrum Master  
**다음 핸드오프**: Dev 에이전트  
**참조 문서**:

- `mockdowns/artifacts/orchestrator/2025-10-28_PRD_summary_v1.0.md`
- `mockdowns/artifacts/analyst/2025-10-28_반복일정기능_PRD_v1.0.md`
- `mockdowns/artifacts/pm/2025-10-28_반복일정기능_로드맵_v1.0.md`
- `mockdowns/artifacts/architect/2025-10-28_반복일정기능_아키텍처설계_v1.0.md`
- `mockdowns/artifacts/2025-10-28_project_structure_v1.0.md`
